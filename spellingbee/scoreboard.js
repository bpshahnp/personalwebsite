import {db,collection,query,orderBy,onSnapshot} from './firebase.js';
const grid=document.getElementById('scoreGrid');onSnapshot(query(collection(db,'teams'),orderBy('score','desc')),snap=>{grid.innerHTML=snap.docs.map((d,i)=>{const t=d.data();return `<article class="score-card ${i===0?'leader':''}"><span class="rank">#${i+1}</span><div><h3>${esc(t.name)}</h3><small>House / Team</small></div><strong>${Number(t.score||0)}</strong><span class="pts">PTS</span></article>`}).join('')||'<div class="empty-manage">No teams added yet.</div>';});function esc(v){return String(v??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));}
